{
  "name": "hmi-agent",
  "scripts": {
    "start": "cd backend && npm start",
    "install": "cd backend && npm install",
    "start:azure": "node server.js",
    "install:azure": "npm install"
  }
}